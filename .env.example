# Galaxy credentials (required for live mode). Leave blank to stay in mock mode.
VITE_GALAXY_API_KEY=api_key_iatest
VITE_GALAXY_API_SECRET_KEY=GaLxAiDviTS12*

# SmartConfig endpoint + credentials. Required in live mode.
VITE_SMARTCONFIG_URL=https://smartconfig.dvetech.fr/configuration/getall
VITE_SMARTCONFIG_LOGIN=app_playup
VITE_SMARTCONFIG_PASS=APpLAyKlis49*
VITE_SMARTCONFIG_APPLICATION_ID=361
VITE_SMARTCONFIG_WANTED_VERSION=2

# Galaxy targeting fallbacks (used when SmartConfig doesn't return them).
VITE_GALAXY_CAMPAIGN_ID=5010
VITE_GALAXY_RUBRIC_ID=297350
VITE_GALAXY_LANGUAGE_CODE=FR
VITE_GALAXY_COUNTRY_CODE=FR

# Vite dev proxy upstreams (used when SmartConfig URL points at /smartconfig-proxy/...
# and the returned galaxyBaseUrl points at /galaxy-proxy)
VITE_SMARTCONFIG_PROXY_TARGET=https://smartconfig.dvetech.fr
VITE_GALAXY_PROXY_TARGET=https://galaxy-api.galaxydve.com

# Supabase (already provisioned)
VITE_SUPABASE_URL=
VITE_SUPABASE_SUPABASE_ANON_KEY=
